import { useState } from "react"
import { toast } from "react-toastify";
import { IonButton, IonSkeletonText, IonText, IonInput, IonCard, IonCardContent, IonCardHeader, IonCardTitle, IonBadge, IonIcon } from "@ionic/react";
import { flashOutline, walletOutline, checkmarkCircle, closeCircle } from "ionicons/icons";
import { TransactionSwapQuote } from "../../../Api/pub/autogenerated/ts/types";
import AmountInput from "@/Components/AmountInput";
import type { AppApiError } from "@/State/api/api";
import { DashErrorBanner } from "../DashErrorBanner";
import { useDashboardSource } from "../DashboardSourceContext";
import {
	useGetAdminTransactionSwapQuotesMutation,
	useListAdminTxSwapsQuery,
	usePayAdminTransactionSwapMutation,
} from "../pubDashApi";



export default function TxSwaps() {
	const { sourceId } = useDashboardSource();
	const { data, error, isFetching, refetch } = useListAdminTxSwapsQuery({ sourceId });
	const [getQuotes, quotesMut] = useGetAdminTransactionSwapQuotesMutation();
	const [paySwap, payState] = usePayAdminTransactionSwapMutation();

	const [amount, setAmount] = useState<string>("");
	const [quotes, setQuotes] = useState<TransactionSwapQuote[]>([]);
	const [address, setAddress] = useState<string>("");

	const loading = !data && (isFetching || !error);

	const requestQuote = async () => {
		try {
			const res = await getQuotes({ sourceId, amountSats: +amount }).unwrap();
			setQuotes(res.quotes);
		} catch (e) {
			toast.error((e as AppApiError).message || "Failed to fetch quotes");
		}
	};

	const doSwap = async (swapOpId: string) => {
		if (!address) {
			toast.error("No address found");
			return;
		}
		try {
			await paySwap({ sourceId, address, swap_operation_id: swapOpId }).unwrap();
			setQuotes([]);
		} catch (e) {
			toast.error((e as AppApiError).message || "Failed to pay swap");
		}
	};
	const cancelSwap = () => {
		setQuotes([]);
	};


	const oldQuotes: TransactionSwapQuote[] =
		data && "quotes" in data && Array.isArray((data as { quotes?: TransactionSwapQuote[] }).quotes)
			? (data as { quotes: TransactionSwapQuote[] }).quotes
			: [];
	const completedSwaps = data?.swaps ?? [];

	return (
		<>
			{error && (
				<DashErrorBanner
					message={(error as AppApiError).message || "Failed to fetch swaps"}
					onRetry={() => void refetch()}
				/>
			)}
			{quotes.length === 0 && (
				<IonCard>
					<IonCardHeader>
						<IonCardTitle>Request New Transaction Swap Quote</IonCardTitle>
					</IonCardHeader>
					<IonCardContent>
						<div style={{ display: 'flex', flexDirection: 'column', gap: '12px' }}>
							<IonText color="medium">
								Amount received by destination address (in sats)
							</IonText>
							<AmountInput
								displayValue={amount}
								effectiveSats={null}
								onType={(text) => { setAmount(text); return text; }}
								unit="sats"
								onToggleUnit={() => { }}
							/>
							<IonButton
								expand="block"
								onClick={() => void requestQuote()}
								disabled={!amount || Number(amount) <= 0 || quotesMut.isLoading}
							>
								<IonIcon icon={flashOutline} slot="start" />
								{quotesMut.isLoading ? "Fetching…" : "Get Quotes from Services"}
							</IonButton>
						</div>
					</IonCardContent>
				</IonCard>
			)}
			{quotes.length > 0 && (
				<>
					<IonText className="ion-margin-top" style={{ fontSize: "1.4rem", fontWeight: "bold", display: "block", marginBottom: "12px" }}>
						<IonIcon icon={flashOutline} style={{ marginRight: "8px" }} />
						New Quotes Available
					</IonText>
					{quotes.map(quote => (
						<IonCard key={quote.swap_operation_id} style={{ border: '2px solid var(--ion-color-primary)' }}>
							<IonCardHeader>
								<div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}>
									<IonCardTitle style={{ fontSize: '1rem' }}>
										{quote.service_url}
									</IonCardTitle>
									<IonBadge color="primary">New Quote</IonBadge>
								</div>
							</IonCardHeader>
							<IonCardContent>
								<div style={{ display: 'flex', flexDirection: 'column', gap: '12px' }}>
									<div style={{
										padding: '12px',
										background: 'var(--ion-color-primary-tint)',
										borderRadius: '8px',
										marginBottom: '8px'
									}}>
										<div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: '8px' }}>
											<IonText><strong>Sent to address:</strong></IonText>
											<IonText><strong>{quote.transaction_amount_sats.toLocaleString()} sats</strong></IonText>
										</div>
										<div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: '8px' }}>
											<IonText color="warning"><strong>Swap + chain fee:</strong></IonText>
											<IonText color="warning"><strong>{(quote.swap_fee_sats + quote.chain_fee_sats).toLocaleString()} sats</strong></IonText>
										</div>
										<div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: '8px' }}>
											<IonText><strong>Service fee:</strong></IonText>
											<IonText><strong>{quote.service_fee_sats.toLocaleString()} sats</strong></IonText>
										</div>
										<div style={{ display: 'flex', justifyContent: 'space-between' }}>
											<IonText><strong>Total spent:</strong></IonText>
											<IonText><strong>{(quote.service_fee_sats + quote.invoice_amount_sats).toLocaleString()} sats</strong></IonText>
										</div>
									</div>
									<div style={{ fontSize: '0.85rem' }}>
										<div style={{ marginBottom: '4px' }}>
											<IonText color="medium">Destination address to send to:</IonText>
										</div>
										<IonInput
											label="Address"
											value={address}
											onIonChange={(e) => setAddress(e.detail.value || "")}
											placeholder="Enter on-chain address"
											style={{
												marginTop: '4px',
												background: 'var(--ion-color-light)',
												borderRadius: '4px',
												fontFamily: 'monospace',
												fontSize: '0.85rem'
											}}
										/>
									</div>
									<IonButton
										expand="block"
										onClick={() => void doSwap(quote.swap_operation_id)}
										disabled={!address?.trim() || payState.isLoading}
									>
										<IonIcon icon={flashOutline} slot="start" />
										{payState.isLoading ? "Executing…" : "Execute Swap"}
									</IonButton>
								</div>
							</IonCardContent>
						</IonCard>
					))}
					<IonButton
						expand="block"
						fill="outline"
						onClick={cancelSwap}
						style={{ marginTop: '8px', marginBottom: '16px' }}
					>
						Cancel & Request New Amount
					</IonButton>
				</>
			)}

			{loading ? (
				<IonCard>
					<IonCardHeader>
						<IonCardTitle>Swap Operations</IonCardTitle>
					</IonCardHeader>
					<IonCardContent>
						<IonSkeletonText animated style={{ width: "40%" }} />
						<IonSkeletonText animated style={{ width: "28%", marginTop: 8 }} />
					</IonCardContent>
				</IonCard>
			) : data && (
				<>
					<IonCard>
						<IonCardHeader>
							<div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}>
								<IonCardTitle>Swap Operations</IonCardTitle>
								<IonButton size="small" fill="outline" onClick={() => void refetch()} disabled={isFetching}>
									Refresh
								</IonButton>
							</div>
						</IonCardHeader>
						<IonCardContent>
							{(oldQuotes.length > 0 || completedSwaps.length > 0) && (
								<IonText color="medium" style={{ fontSize: '0.9rem' }}>
									{oldQuotes.length > 0 && <>Pending quotes: {oldQuotes.length}</>}
									{oldQuotes.length > 0 && completedSwaps.length > 0 && " · "}
									{completedSwaps.length > 0 && <>Completed: {completedSwaps.length}</>}
								</IonText>
							)}
						</IonCardContent>
					</IonCard>

					{oldQuotes.length === 0 && completedSwaps.length === 0 && quotes.length === 0 && (
						<IonCard>
							<IonCardContent style={{ textAlign: 'center', padding: '32px' }}>
								<IonIcon
									icon={walletOutline}
									style={{ fontSize: '64px', color: 'var(--ion-color-medium)', marginBottom: '16px' }}
								/>
								<IonText color="medium">
									<h3>No swap operations yet</h3>
									<p>Request a quote above to get started with transaction swaps</p>
								</IonText>
							</IonCardContent>
						</IonCard>
					)}

					{oldQuotes.length > 0 && (
						<>
							<IonText className="ion-margin-top" style={{ fontSize: "1.2rem", fontWeight: "bold", display: "block", marginBottom: "8px" }}>
								Pending Quotes
							</IonText>
							{oldQuotes.map(quote => (
								<IonCard key={quote.swap_operation_id}>
									<IonCardHeader>
										<IonCardTitle style={{ fontSize: '1rem' }}>{quote.service_url}</IonCardTitle>
									</IonCardHeader>
									<IonCardContent>
										<div style={{ display: 'flex', flexDirection: 'column', gap: '8px' }}>
											<div style={{ display: 'flex', justifyContent: 'space-between' }}>
												<IonText color="medium">Sent to address:</IonText>
												<IonText><strong>{quote.transaction_amount_sats.toLocaleString()} sats</strong></IonText>
											</div>
											<div style={{ display: 'flex', justifyContent: 'space-between' }}>
												<IonText color="medium">Swap + chain fee:</IonText>
												<IonText><strong>{(quote.swap_fee_sats + quote.chain_fee_sats).toLocaleString()} sats</strong></IonText>
											</div>
											<div style={{ display: 'flex', justifyContent: 'space-between' }}>
												<IonText color="medium">Total spent:</IonText>
												<IonText><strong>{(quote.service_fee_sats + quote.invoice_amount_sats).toLocaleString()} sats</strong></IonText>
											</div>
											<IonInput
												label="Address"
												value={address}
												onIonChange={(e) => setAddress(e.detail.value || "")}
												placeholder="Enter on-chain address"
												style={{ marginTop: '8px' }}
											/>
											<IonButton size="small" onClick={() => void doSwap(quote.swap_operation_id)} disabled={!address?.trim() || payState.isLoading}>
												{payState.isLoading ? "Executing…" : "Execute Swap"}
											</IonButton>
										</div>
									</IonCardContent>
								</IonCard>
							))}
						</>
					)}

					{completedSwaps.length > 0 && (
						<>
							<IonText className="ion-margin-top" style={{ fontSize: "1.2rem", fontWeight: "bold", display: "block", marginBottom: "8px" }}>
								Completed Swaps
							</IonText>
							{completedSwaps.map(op => (
								<IonCard key={op.quote.swap_operation_id}>
									<IonCardContent>
										<div style={{ display: 'flex', alignItems: 'center', gap: '8px', flexWrap: 'wrap' }}>
											{!op.failure_reason ? (
												<>
													<IonIcon icon={checkmarkCircle} color="success" style={{ fontSize: '24px' }} />
													<IonText>Success: {op.operation_payment?.amount?.toLocaleString()} sats to {op.address_paid ?? '—'}</IonText>
												</>
											) : (
												<>
													<IonIcon icon={closeCircle} color="danger" style={{ fontSize: '24px' }} />
													<IonText color="danger">Failed: {op.operation_payment?.amount?.toLocaleString()} sats to {op.address_paid ?? '—'} — {op.failure_reason}</IonText>
												</>
											)}
										</div>
									</IonCardContent>
								</IonCard>
							))}
						</>
					)}
				</>
			)}
		</>
	);
}
