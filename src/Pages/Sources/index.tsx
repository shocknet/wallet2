import { PageProps } from "../../globalTypes";
import { Fragment } from "react";

export const Sources: React.FC<PageProps> = (): JSX.Element => {
  return(
    <Fragment>
      <section className="Sources">
        <h2 className="Sources_title">test</h2>
      </section>
    </Fragment>
  )
}